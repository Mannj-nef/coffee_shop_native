import * as React from 'react'
import { StatusBar } from 'react-native'
import { COLORS } from '../../themes'

const StatusBarComponent = () => {
  return <StatusBar backgroundColor={COLORS.primaryBlackHex} />
}

export default StatusBarComponent
